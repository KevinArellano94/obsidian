#git #initialize #new #02-20-2025  

## New repository

```bash
git init
git add README.md
git commit -m "..."
git branch -M master
git remote add origin git@github.com:KevinArellano94/obsidian.git
git push -u origin master
```

### Oneliner

```bash
git commit -m "..."; git branch -M master; git remote add origin git@github.com:KevinArellano94/obsidian.git; git push -u origin master
```

## Remove files / directories

```bash
git rm -r ...
```